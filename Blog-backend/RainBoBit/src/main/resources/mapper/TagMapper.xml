<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wybbb.rainbobit.mapper.TagMapper">
    <delete id="deleteByArticleId">
        DELETE FROM rbb_article_tag
        WHERE article_id = #{articleId}
    </delete>

    <select id="relateToArticle" parameterType="java.lang.Long" resultType="java.lang.Long">
        select count(*)
        from rbb_article_tag
        where tag_id = #{tagId}
    </select>

    <insert id="saveTagsBatch">
        INSERT INTO rbb_article_tag (article_id, tag_id)
        VALUES
        <foreach collection="tagIds" item="tagId" separator=",">
            (#{articleId}, #{tagId})
        </foreach>
    </insert>

    <select id="getTagsBatch" resultType="java.lang.Long">
        select tag_id
        from rbb_article_tag
        where article_id = #{articleId}
    </select>
</mapper>